{
  "name": "sjmb-scraper-worker",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "node-html-parser": "^6.1.0"
  }
}
